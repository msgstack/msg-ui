import React from 'react'

interface FooterProps {
  children?: React.ReactNode
}

const Footer: React.FC<FooterProps> = (props) => {
  return <div className="p-workspace__footer"></div>
}

export default Footer
